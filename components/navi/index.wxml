<view class="container">
  <image class="icon" src="{{latest?disLeftSrc:LeftSrc}}" bind:tap="onPrev"></image>
  <text class="title">{{title}}</text>
  <image class="icon" src="{{first?disRightSrc:RightSrc}}" bind:tap="onNext"></image>
</view>